&lt; integration_rules &gt;     &lt;provider&gt; Selcom  Tanzania&lt;/provider&gt;     &lt;environment&gt;         &lt;setting name=" base_url " value="https://api.selcom.co.tz/v1" /&gt;         &lt; security_rule &gt;All API keys and secrets must remain on the backend server. Never expose the  Selcom  API key or secret token to the frontend repository.&lt;/ security_rule &gt;     &lt;/environment&gt;     &lt; architecture_constraints &gt;         &lt;constraint name="idempotency"&gt;             The AI must generate a unique ' trans_id ' on our backend for every checkout initialization. If  Selcom  returns an error or a timeout, the retry logic must use the exact same ' trans_id ' to prevent double-charging the customer's mobile wallet.         &lt;/constraint&gt;         &lt;constraint name="webhooks"&gt;             The AI must create a dedicated, unauthenticated POST endpoint for  Selcom  webhooks. This endpoint must validate  Selcom's  digital signature before updating the database order status to 'Paid'.         &lt;/constraint&gt;         &lt;constraint name=" status_polling "&gt;             Because mobile money transactions rely on USSD push prompts on the user's phone, the frontend must poll a secure internal backend endpoint to check the transaction status, rather than calling  Selcom  directly.         &lt;/constraint&gt;     &lt;/ architecture_constraints &gt; &lt;/ integration_rules &gt;